export function createMcpServer(): never {
  throw new Error('MCP server not yet implemented');
}

export function startMcpServer(): void {
  console.error('MCP server not yet implemented. Use CLI commands instead.');
  process.exit(1);
}
