{
  "hooks": [
    {
      "matcher": "PostToolUse",
      "hooks": [
        {
          "type": "command",
          "command": "npx no-tickets push --dry-run 2>/dev/null | grep -q '\"entities\"' && npx no-tickets push || true",
          "description": "Auto-push to no-tickets when .notickets/ files change"
        }
      ]
    }
  ]
}
